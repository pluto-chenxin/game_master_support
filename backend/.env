# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# PostgreSQL connection string for local development
DATABASE_URL="postgresql://postgres:admin@localhost:5432/game_master_db"
# For Production: Use PostgreSQL in AWS
# DATABASE_URL format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# Email Configuration
EMAIL_DISABLE_SENDING=true
EMAIL_FROM="Game Master Support <support@gamemasterapp.com>"

# Authentication
JWT_SECRET="BZ;6]EcU>Cd^f3p]9]kG2RtKI+LjEI"
JWT_EXPIRATION="7d"

# Frontend URL
FRONTEND_URL="http://localhost:3000"

# For testing email with Gmail
# Replace with your actual credentials if needed for testing
EMAIL_HOST="smtp.gmail.com"
EMAIL_PORT=587
EMAIL_USER="your-email@gmail.com"
EMAIL_PASSWORD="your-app-password"
EMAIL_SECURE=false

# For production environment (these will be set in AWS)
# DATABASE_URL will be set to the AWS RDS PostgreSQL instance
# Example: postgresql://gamemaster:password@your-instance.rds.amazonaws.com:5432/game_master_db
# JWT_SECRET should be a strong random string in production
# FRONTEND_URL will be your Amplify app URL